[user]
	email = philip.herron@googlemail.com
	name = Philip Herron
[color]
        ui = auto
[color "branch"]
        current = yellow reverse
        local = yellow
        remote = green
[color "diff"]
       meta = yellow bold
       frag = magenta bold
       old = red bold
       new = green bold
[color "status"]
       added = yellow
       changed = green
       untracked = cyan
[alias]
	conflicts = diff --name-only --diff-filter=U
